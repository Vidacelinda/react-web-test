import Message from './Message' ;
//have the message compnnent in here too

//component
function App(){
  //close message componet
  return <div><Message/></div>
}
//export App compnet so it could be used somewhere else
export default App;

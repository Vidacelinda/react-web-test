

//PascalCasing function casing upp
function Message(){
    //JSX:javascript XML (this code will get compiled down to java script)
    const name='Carl'
    if(name)
        return <h1>Hello {name}</h1>;//you can also call a function like 'getName()'
    return <h1>Test1 Message component </h1>;
}
export default Message
